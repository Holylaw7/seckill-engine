# Phase 5 测试配置隔离（AbstractIntegrationTest 动态覆盖 datasource/redis）
server:
  port: 0
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seckill_auth_test
    username: test
    password: test
  data:
    redis:
      host: localhost
      port: 6379
seckill:
  auth:
    worker-id: 21
    jwt:
      secret: test-secret
      expire-minutes: 120
